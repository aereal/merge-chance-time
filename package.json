{
  "name": "merge-chance-time",
  "version": "1.0.0",
  "repository": "git@github.com:aereal/merge-chance-time.git",
  "author": "aereal <aereal@aereal.org>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace admin-front build",
    "dev": "yarn workspace admin-front dev",
    "test": "yarn workspace admin-front test"
  }
}
